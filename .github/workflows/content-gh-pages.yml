name: Deploy content to GitHub Pages
on:
  push:
    branches:
      - main
    paths:
      - test-content/**/*
      - .github/workflows/**
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2
      - name: Install Jekyll
        run: gem install jekyll
      - name: Prepare build directory
        run: mkdir build-root && mv test-content build-root/
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: build-root
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy content to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
